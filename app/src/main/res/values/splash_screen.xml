<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@color/primary</item>

        <!-- Use your app logo as the splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_channel_foreground</item>

        <!-- The theme that will be used after the splash screen disappears -->
        <item name="postSplashScreenTheme">@style/Theme.Spenso</item>
    </style>
</resources>